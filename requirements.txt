# ---------------------------------------------------------------------------
# Runtime dependencies (pinned). Regenerate with:
#   pip install --upgrade <pkg> && pip freeze
# ---------------------------------------------------------------------------

# --- Web framework ---------------------------------------------------------
Flask==3.1.3
Werkzeug==3.1.8
gunicorn==26.0.0

# --- Database --------------------------------------------------------------
SQLAlchemy==2.0.51
Flask-SQLAlchemy==3.1.1
alembic==1.18.5
Flask-Migrate==4.1.0
psycopg[binary,pool]==3.3.4

# --- Redis: cache, rate limiting, sessions ---------------------------------
# redis-py is held at 7.4.1, not 8.1.0: Flask-Limiter's storage layer (`limits`,
# currently 5.8.0) declares `redis<8.0.0`, so 8.x makes the graph unresolvable.
# This caps the *client* only — the Redis server in Compose is 8.x.
# Revisit when `limits` lifts the bound: https://github.com/alisaifee/limits
redis==7.4.1
Flask-Caching==2.4.1
Flask-Limiter[redis]==4.1.1

# --- Configuration & validation --------------------------------------------
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0
python-dotenv==1.2.2

# --- Cross-cutting ---------------------------------------------------------
Flask-Cors==6.0.5
structlog==26.1.0
